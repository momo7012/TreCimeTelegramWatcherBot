# Railway setup

1. Create a new project from this folder / GitHub repo.
2. Add one environment variable:
   TELEGRAM_BOT_TOKEN=<token from BotFather>
3. Deploy.
4. Open Telegram and send /start to your bot.
5. Send /check once. If it returns HTTP 200, the watcher is working.
